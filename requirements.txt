# Exact versions of the environment the results were produced in.
# Python 3.14 on Windows (local development) and Linux (A100 workstation).
# The full resolved tree, including transitive dependencies, is in
# requirements.lock.txt — regenerate both with pip freeze after any change.
#
# Note: on Windows these torch wheels are CPU-only, which is what the Docker
# image needs anyway; on Linux the same pins pull the CUDA build.

# Deep learning
torch==2.13.0
torchvision==0.28.0
ultralytics==8.4.128

# Data / vision
numpy==2.5.2
opencv-python-headless==5.0.0.93
pillow==12.3.0
pycocotools==2.0.11
pyyaml==6.0.3

# Analysis and figures
pandas==3.0.5
matplotlib==3.11.1

# API and demo
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32

# Report generation
reportlab==5.0.1

# Utilities
tqdm==4.70.0
